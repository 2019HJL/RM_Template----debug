片上外设
	gpio
	spi
	pwm
	tim（用于中断）
	usart
	can
	dma
	sdio(后续添加)
	usbio(后续添加)
	iic(后续添加)
	adc(后续添加)
	dac(后续添加)
板载外设
	beep
	led
	key
	mpu6500(不用ist8310)
		heat(IMU温控)
	SDcard(后续添加)
	usb(后续添加)
外挂设备
	3508(底盘)
	6623(云台)
	2312(摩擦轮)
	拨弹电机
	其他电机
	laser(激光)
	单轴陀螺仪
	UWB(定位)
	裁判主控(读数据)
	DR16(遥控)
	妙算（视觉辅助）
算法
	crc(校验裁判系统的数据)
	pid(单、双环)
		ramp(平滑键鼠模式的速度突增)
	弹丸热量计算
	底盘功率限制（试）
	buff计算（能量机关、RFID）
	等级计算
	多车数据汇总（wifi，后续）
	麦伦运动计算
	云台控制
	供弹控制
系统监视
	错误处理
	错误状态指示
	看门狗
	系统复位
基础支持(采用原子例程)
	sys
	delay

调试接口
	自己仿的简易usmart（参数传输）
	匿名四轴（波形、姿态查看）
